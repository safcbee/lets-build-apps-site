# Internal working material

This directory is for non-published working material: audits, reports, design exports, experiments, and historical notes.

The production website artifact is `../public/` only. GitHub Pages should be configured to deploy `public/` via the Pages GitHub Actions workflow, not from the repository root.

Do not copy internal material into `public/` unless it is intentionally part of the production website.
